# Static Site Generator

A simple static site generator built in Ruby that converts Markdown files into HTML websites.

## Features

- Read Markdown files from a folder
- Convert Markdown content into HTML
- Wrap HTML content inside customizable templates
- Generate static HTML files (including index.html) for deployment
